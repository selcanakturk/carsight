# Comma-separated browser origins allowed to call the API.
# Do not include paths; include each origin's scheme and hostname.
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://your-project.vercel.app
